{
  "name": "questify",
  "private": true,
  "scripts": {
    "dev:nuxt": "pnpm --filter nuxt dev",
    "dev:worker": "pnpm --filter worker dev",
    "prisma:generate": "pnpm --filter prisma generate",
    "db:migrate": "pnpm --filter prisma migrate",
    "db:seed": "pnpm --filter prisma seed",
    "db:reset": "pnpm --filter prisma exec -- prisma migrate reset --force && pnpm db:seed",
    "prepare": "pnpm --filter prisma prepare",
    "build": "pnpm --parallel build"
  }
}
